[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "battle-pkg"
version = "1.0.0"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["battle_pkg"]
